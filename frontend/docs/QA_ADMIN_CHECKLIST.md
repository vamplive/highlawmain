# 관리자 페이지 수동 QA 체크리스트

> **목적**: 관리자 페이지 13개 메뉴의 핵심 업무 흐름(CRUD · 파일 I/O · 공개 페이지 연동)을 수동으로 전수 검증.
> **선행 조건**: 백엔드(5001) · 프론트엔드(5173) 실행 중, admin 계정(`admin / 0524`)으로 로그인.
> **사용 방법**: 항목마다 실제로 조작해보고 결과를 체크. 실패 항목은 하단 "발견된 이슈"에 기록.

---

## 공통 검증 수칙

- **실데이터 오염 방지**: 테스트로 생성한 레코드는 삭제 또는 명확한 프리픽스(예: `[QA]`)로 표시.
- **브라우저 콘솔 항상 열어두기** (F12): 경고·에러 발생 시 즉시 인지.
- **Network 탭 확인**: 저장 시 4xx/5xx 없는지, 응답 바디가 `{data, error, meta}` 형식인지.
- **DB 직접 검증(선택)**: `sqlite3 backend/data/db/yjlaw.db` 로 저장 반영 재확인.

---

## Overview 섹션

### ① `/admin` — 대시보드

- [ ] 전체 문서 / 금주 신규 / 열람 중 / 완독 처리 카드 4개 숫자 표시
- [ ] 문서 유형 분포(법령/판례/교과서/서적/논문/뉴스/메모) 진행 바 렌더
- [ ] 최근 등록 문서 테이블에 최소 1건 이상 표시
- [ ] 각 문서 행 클릭 시 해당 문서 편집/상세로 이동 (동작 정의되어 있다면)

### ② `/admin/analytics` — 분석

- [ ] 기간 선택 버튼(7일/30일/90일) 클릭 시 데이터 갱신 (Network 탭에서 다른 `period=` 쿼리 관찰)
- [ ] 요약 카드 4개(조회수/순방문자/일 평균/전환율) 숫자 정상
- [ ] 일별 조회수 라인 차트 렌더 (Canvas)
- [ ] 인기 페이지 바 차트 렌더 (Canvas) — 데이터 있을 시 `item.views` 기반
- [ ] 유입 경로 테이블 또는 "없습니다" 빈 상태 표시
- [ ] **CSV 내보내기** 버튼 클릭 → `analytics-30d.csv` 다운로드되고 열람 시 헤더 + 행 데이터 존재

---

## Casework 섹션

### ③ `/admin/bookings` — 예약

- [ ] **예약 현황** 탭: 날짜별 예약 목록 또는 빈 상태
- [ ] **예약 설정** 탭: 주별 가능 시간대, 휴일, 상담 유형 설정
- [ ] 설정 변경 후 저장 → 공개 상담 페이지(`/consultation`) 가용 시간대에 반영되는지 확인
- [ ] 예약 상세 진입 시 고객 정보/메모 표시
- [ ] 예약 상태 변경(확정/취소) 반영

### ④ `/admin/clients` — 고객

- [ ] 고객 목록 테이블 · 총원 카운트 정확
- [ ] **+ 고객 등록** → 이름/전화번호/이메일/분야/출처 입력 → 저장
  - [ ] 필수 필드 비우고 저장 시도 → 검증 에러 표시
  - [ ] 성공 시 목록에 즉시 추가
- [ ] **수정** → 필드 변경 → 저장 → 목록 반영
- [ ] **비활성** 토글 → 상태 변경, 비활성 고객 필터링 동작
- [ ] **삭제** → 확인 프롬프트 → 목록에서 제거
- [ ] 공개 상담 신청(`/consultation`) 완료 시 고객이 자동 등록되는지 확인

### ⑤ `/admin/cases` — 사건

- [ ] 사건 목록 · 빈 상태 "등록된 사건이 없습니다" ⚖️
- [ ] **+ 사건 등록**
  - [ ] 고객 선택 드롭다운에 기존 고객 표시
  - [ ] 사건명/카테고리/상태/담당 변호사/메모 입력
  - [ ] 저장 시 목록 반영
- [ ] 사건 상세 진입 → 타임라인/문서/메시지 탭 존재 여부
- [ ] 상태 변경(접수/진행 중/종결) 반영
- [ ] 고객 삭제 시 연관 사건 처리 정책 (cascade / soft)

### ⑥ `/admin/messages` — 메시지 발송

- [ ] **템플릿 관리** 탭
  - [ ] **+ 새 템플릿** → 채널(SMS/이메일) · 제목 · 본문 입력 → 저장
  - [ ] 템플릿 수정/삭제
- [ ] **메시지 발송** 탭
  - [ ] 고객 선택 (다중 선택 가능?)
  - [ ] 템플릿 선택 → 본문 미리보기
  - [ ] 발송 버튼 클릭 → 확인 모달 → 발송
  - [ ] **⚠️ 주의**: 실제 SMS/이메일이 나감 — 테스트용 번호·주소로만
- [ ] **발송 이력** 탭: 방금 발송한 기록 표시(상태: 성공/실패)

---

## Content 섹션

### ⑦ `/admin/editor` — 에디터

- [ ] 리본 8탭(파일/홈/삽입/그리기/디자인/레이아웃/참조/검토/보기) 전환 동작
- [ ] 문서 트리 사이드바 표시 (`100_노트`, `200개 문서` 등)
- [ ] 타이핑 → 화면 반영
- [ ] **Ctrl+S** → 서버 저장 → 우측 상단 또는 토스트로 성공 표시
- [ ] **Ctrl+B/I/U** → 굵게/기울임/밑줄 적용
- [ ] **삽입 탭** → 표 격자 8x10 선택 → 표 삽입
- [ ] **삽입 탭** → 이미지 URL/파일 삽입
- [ ] **파일 탭(백스테이지)** → 내보내기(.docx/.pdf/.html) 각각 다운로드
- [ ] **파일 탭** → 열기(.docx) → 기존 Word 파일 불러오기 → 서식 보존
- [ ] 자동 저장 동작 (localStorage 키 `word-editor-autosave` 갱신 확인)
- [ ] 댓글 삽입 / 각주 삽입 → 사이드 패널/하단 영역 표시
- [ ] 콘솔 경고 "Duplicate extension names" 발생 — 기존 이슈 (별도 처리)

### ⑧ `/admin/documents` — 문서

- [ ] 목록 페이지네이션(11페이지 등) 이동 동작
- [ ] **필터**: 모든 유형 / 법령 / 판례 … 드롭다운 반영
- [ ] **필터**: 상태(수신함/읽는 중/완독/보관/참고) 반영
- [ ] **검색**: 제목 키워드 입력 시 결과 필터링 (FTS5)
- [ ] **+ 새 문서 등록** → 모달/페이지에서 제목 · 유형 · 중요도 입력 → 저장
- [ ] **수정** → 폼에서 변경 → 저장 → 목록 반영
- [ ] **삭제** 1차 → archived 상태로 이동 (소프트 삭제)
- [ ] **삭제** 2차 → 영구 삭제 확인 프롬프트
- [ ] 행 클릭 → 문서 상세/에디터로 이동
- [ ] 파일 업로드(PDF/MD/TXT/HTML) → 자동 분석 후 문서 생성

### ⑨ `/admin/site-manager` — 홈페이지 관리

- [ ] 10개 서브탭(홈페이지/히어로 영상/소개/업무분야/공통/테마/SEO/공지/배너/개발 일지) 전환
- [ ] 한국어/English 언어 전환 → 해당 언어 폼 표시
- [ ] **홈페이지 탭**
  - [ ] 제목/부제목/태그라인 변경 → 저장 → 공개 홈페이지(`/`) 즉시 반영
  - [ ] CTA 버튼 텍스트/링크 변경 저장
  - [ ] 주요 지표 추가/제거
  - [ ] 접근 방식 하이라이트 추가/제거
- [ ] **히어로 영상 탭** → 영상 업로드/활성 영상 변경 → 홈페이지 히어로 영상 반영
- [ ] **소개/업무분야 탭** → 섹션별 텍스트 수정 저장
- [ ] **공통(헤더/푸터) 탭** → 연락처 · 주소 · 사업자 정보 저장
- [ ] **테마 탭** → 기본 색상 토큰 변경 (위험 — 공개 페이지 즉시 영향)
- [ ] **SEO 탭** → 메타 태그·OG 태그 변경 → 공개 페이지 `<head>` 반영
- [ ] **공지/배너 탭** → 활성 공지 등록 → 공개 페이지 상단 배너 표시
- [ ] **개발 일지 탭** → 로그 읽기 전용 표시

### ⑩ `/admin/reviews` — 후기

- [ ] 빈 상태 "등록된 후기가 없습니다" 📝
- [ ] **+ 후기 등록** → 고객명/내용/별점 입력 → 저장
- [ ] 공개/비공개 토글 → 공개 후기 페이지(`/reviews`) 즉시 반영
- [ ] 수정/삭제

### ⑪ `/admin/media` — 미디어

- [ ] 폴더 트리(전체) 표시
- [ ] **파일 드래그 앤 드롭** 업로드 → 업로드 진행 표시 → 그리드 추가
- [ ] **클릭 업로드** 동작 확인
- [ ] 50MB 이하 파일만 허용 → 초과 시 에러 메시지
- [ ] 카테고리 필터(이미지/영상/문서) 동작
- [ ] 파일 클릭 → 상세 모달 → URL 복사 · 다운로드 · 삭제
- [ ] 삭제 → 그리드에서 제거, 참조된 에디터/홈페이지에서 끊김 처리 확인

---

## Firm 섹션

### ⑫ `/admin/lawyers` — 변호사

- [ ] 4명(윤세환/한샘이/김수경/김효림) 카드 정렬 순서(#0~#3) 유지
- [ ] 👁️ 아이콘 → 공개 미리보기 모달
- [ ] ✏️ 아이콘 → 수정 폼(이름/직책/학력/경력/전문분야/사진/이메일/전화 등)
- [ ] 사진 업로드 또는 URL 설정 → 저장 → 공개 변호사 페이지(`/lawyers`) 반영
- [ ] 🗑️ 아이콘 → 삭제 확인 → 목록에서 제거
- [ ] **+ 변호사 등록** → 신규 등록 → 목록·공개 페이지 반영
- [ ] 정렬 순서(sort_order) 변경 → 공개 페이지 순서 반영
- [ ] 비활성(is_active=0) 토글 → 공개 페이지에서 숨김

---

## System 섹션

### ⑬ `/admin/settings` — 설정

#### 사용자 관리 탭
- [ ] admin 사용자 1명 표시
- [ ] **+ 사용자 등록** → 아이디/비밀번호(8자 이상)/이름/역할/이메일 → 저장
  - [ ] 8자 미만 비밀번호 거부
  - [ ] 중복 아이디 거부
- [ ] **수정** → 이름/이메일/역할/활성 상태 변경 → 저장
- [ ] **비밀번호 변경** → 8자 이상 검증 → 변경 후 해당 계정으로 재로그인 가능
- [ ] **삭제** → 프롬프트 → 목록에서 제거 (본인 계정 삭제 방지 정책 확인)

#### 보안 탭
- [ ] 세션 목록 (현재 접속 중 세션 표시)
- [ ] 세션 강제 종료 동작
- [ ] 2FA/TOTP 설정 UI (있을 경우)

#### 활동 로그 탭
- [ ] 최근 로그인·변경 이벤트 목록 표시
- [ ] 필터(기간/사용자/액션) 동작

#### 개발 이력 탭
- [ ] dev-logs 렌더링 (읽기 전용)

---

## 크로스 페이지 플로우 (E2E 시나리오)

### 시나리오 A: 상담 신청 → 업무화
1. 공개 `/consultation` 페이지에서 상담 신청 제출
2. `/admin/clients` 에 신규 고객 자동 등록 확인
3. `/admin/bookings` 에 예약 생성 여부 (상담 양식에 예약 포함 시)
4. `/admin/messages` 에서 해당 고객에게 확인 메시지 발송
5. `/admin/cases` 에서 해당 고객으로 사건 생성

### 시나리오 B: 문서 제작 → 게시
1. `/admin/editor` 에서 새 문서 작성 → 저장
2. `/admin/documents` 목록에서 방금 저장한 문서 확인
3. 수정 → 다시 저장 → 갱신 시간 반영
4. .docx 내보내기 → .docx 열기 → 서식 유지 확인

### 시나리오 C: 사이트 콘텐츠 실시간 반영
1. `/admin/site-manager` 홈페이지 탭에서 제목 변경 → 저장
2. 새 탭에서 `/` 열기 → 변경사항 반영 (CDN/캐시 이슈 없는지)
3. 테마 탭 색상 변경 → 공개 페이지 반영 (신중)

---

## 비기능 검증

### 인증·권한
- [ ] 로그아웃 → `/admin/*` 재접속 시 로그인 페이지로 리다이렉트
- [ ] 세션 만료(토큰 삭제) 시 401 → 자동 로그아웃
- [ ] URL 직접 입력(`/admin/documents`)으로 비로그인 접근 차단
- [ ] role=editor 사용자가 role=admin 전용 기능 접근 시 차단 (역할 구조 있다면)

### 반응형
- [ ] 1440px(데스크탑) / 1024px(태블릿) / 768px(모바일) 뷰포트에서 사이드바·테이블 동작
- [ ] 사이드바 축소 모드(68px) 아이콘 가운데 정렬 및 툴팁

### 접근성
- [ ] Tab 키로 사이드바 · 폼 · 버튼 순회 가능
- [ ] 폼 필드 label 연결 확인
- [ ] 모달 열릴 때 포커스 트랩, Esc로 닫힘
- [ ] 이미지 alt 속성

### 성능
- [ ] 첫 진입 시 스켈레톤 → 데이터 순서
- [ ] 문서 200개 이상에서 페이지네이션 응답 속도
- [ ] 미디어 업로드 중 UI 멈추지 않음

---

## 발견된 이슈 기록 양식

| # | 페이지 | 심각도 | 재현 경로 | 기대 동작 | 실제 동작 | 비고 |
|---|--------|--------|-----------|-----------|-----------|------|
| 1 |        |        |           |           |           |      |

---

## 이미 알려진 이슈 (2026-04-20 기준)

- **TipTap 중복 확장 경고** (`/admin/editor`): StarterKit v3가 이미 포함하는 `Link`/`Underline`/`TextDirection`을 별도 등록 중. 동작은 정상, 경고만 발생. → 후속 작업으로 분리됨.
- **`/admin/hero-videos` 고아 라우트**: 메뉴에 없지만 URL로 접근 가능. 사이트 관리 하위 서브페이지로 들어가는 설계인지 확인 필요.

---

## 검증 완료 서명

- 검증자:
- 검증 일자:
- 발견 이슈 수: ___건 (Critical ___ / High ___ / Medium ___ / Low ___)
- 전체 평가: ☐ 양호 / ☐ 조건부 통과 / ☐ 추가 수정 필요
